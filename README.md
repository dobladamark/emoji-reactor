# emoji-reactor

A simple JavaScript feature that allows users to click emoji-styled buttons and increase a counter up to a maximum of 10.

🚀 Features

Each emoji button has its own counter

Clicking the button increases the counter

Counter stops automatically once it reaches 10

Lightweight and easy to integrate in any webpage

📌 How It Works

The script does the following:

Selects all elements with the class .emoji-btn

When an emoji button is clicked:

Finds the .count span inside the button

Reads the current count

Increases it by 1

Updates the text

Stops at 10/10
